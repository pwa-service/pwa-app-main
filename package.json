{
  "private": true,
  "name": "pwa-monorepo",
  "version": "0.0.1",
  "workspaces": [
    "pwa-api-gateway",
    "pwa-auth-service",
    "pwa-event-handler",
    "pwa-shared",
    "pwa-protos",
    "pwa-prisma"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run start:dev --workspaces",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "eslint": "^9.14.0",
    "prettier": "^3.3.3"
  }
}

